# Agent Overlay

The app lives here; the documentation lives in the [repository README](../README.md) — overview, install, how status is decided, hooks, controls and roadmap.

Build from this directory:

```sh
npm install
npm run tauri dev      # hot-reload dev build
npm run tauri build    # release bundle
cd src-tauri && cargo test --lib
```
